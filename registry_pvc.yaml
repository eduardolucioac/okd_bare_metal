apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: <OKD_SERVICES_STRG_SZ>Gi
  volumeMode: Filesystem
